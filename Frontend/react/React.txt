React : it is a open source library for building user interfaces. It focuses on only thing like building user interfaces does not focus on the http request or network serving and routing. React has a rich ecosystem that make its capable of making a rich ecosystem. React follows a component where different component are encapsulated to make a official website.

So in a normal page we have 4 section :- header,sidenav,footer,main content. React code should be reusable and then that component can be directly used in other things by passing right data like in vue, angular ,etc.

React has a declarative way means you just need to tell what makes and then it will automatically creates a ui like if you give painter give me a painting of good view then it will automatically create a new ui and other one is imperative way where we need to tell at each step. for example make mountain then river like means we have to tell at each step and in imperative way you control the flow of landscape but in react you give control to other then its there responsibility what to draw what but just completes.

React will handle efficiently updating and rendering of the components when the data changes and it also makes a very good dom update.

Since the react is based on the component either we can integrate one component or other or an entire application to the other applications if we want.

// So what happens in normal js state - js and ui-dom has not any synchronisation so everything proper does not get updated and react solve this problem.
In react the problems are the code that it gives is not a good html css means and so have the problem of seo as it does not generate proper good html css js.
Browser needs to render js so it becomes slow and it has router problem as it makes a single page application. So these are the problems of seo.React does not have state management.

>> Vite or Parcel : They are the bundler we write the js in so many file so it bundles everything and put everything in just one file.

React npx : it is a npm package runner which is installed during node and using that we are able to create react. create-react-app is a utility which is used to create a react app but it has so many things that not need to create hence takes a lot of time.

>> package json : first go to package.json and then it will give you a library for that. it has scripts, react-dom,react,web-vitals(they are used to measure the performance of application)

scripts are basically used to run the react means how it gonna run in a local and how it gonna run in a production and how it gonna run in a test .since the requirement needed to run is different from local

npx create-react-app react-project-name // this will be so heavy and will take a huge amount of time but creating a react project using vite that is a library that helps to create react project with necessary dependencies only.

npm create vite@latest // then project ka name

npm install are used to install dependencies to run project that are present in package.json

>> Project Structure : Main entry point of the react is index.js file so basically react also creates its dom and that is called the virtual dom and that dom has been compared against the main dom that what is the difference and then at that point only it repaints the pixel.

So basically what react gave it gave took a function having html tags and using that html tags will be rendered using the react and it can be passed also. Using js we are writing html that is the power of react.

>> In react basically react-scripts in package.json is responsible to put js in file and loading all the scripts. So in basically all those react-scripts are not there . here it directly put the needed jsx or js file in the index.html directly and not needed to load.

In react when we are rendering element then we should return only one element more than one not allowed so to configure that we can put all in a div or any common thing so to solve this problem <></> we can use these tags where we will enclose all the things together in a tag and this tag is called fragment.

>> In react function name should be capital and also it is best practices otherwise tag will not come while exporting and it is good to create name with capital letter even js or jsx file.

Actually the transpiler like bable or vitr all these convert thse react code into a these structure of properties of any division like means it split the tag into these and this gives the normal like component.


function renderElement(reactElement,container){
    const domElement = document.createElement(reactElement.type);
    domElement.innerHTML = reactElement.children;
    domElement.setAttribute('href',reactElement.props.href)
    domElement.setAttribute('target',reactElement.props.href)
    container.appendChild(domElement)
}

// it breaks the tag into like these element and then data gets filled over there.
const reactElement = {
    type : 'a',
    props : {
        href : "https://google.com",
        target : "_blank"
    },
    children : "Click here to visit google.com"
}
const mainContainer = document.querySelector("#root")

renderElement(reactElement,mainContainer)


>> So basically in a react we follow a component based architecture where each component together form to make a entire UI application and here each component can be reused and these component can be nested component or the parent component. These component are placed inside the js file or jsx file.


>> Why do you nee hooks and all these :-

// so basically for the UI updation we need the hooks in general what happen if we want to update the value then in normal js here we will get the element by id and then change the innerText and all these. So basically all these things will happen so control this properly we mainly use the react hooks proper ui updation.So basically to control the ui updation react hooks are used.

.. usestate Hook : so basically this hook is to change the state it does not mean changing the variable or other thing the change to the dom or you can say UI.

const [counter,setCounter] = useState(//here we can pass any default value integer string any data type object means everything evrn function we can give)
// so this method will give first the variable and second the method to change that variable or other thing we cam give anyname to the variable or function but it is good to have convenient name. IN SETSTATE BROWSER RE-RENDER EVERYTHING.

>> Fibre,Reconciliation : so basically what happens if there are multiple request means counter variable updating again and again means here it is in that like it is in network and then new request comes so new new like goes on so to avoid this what we can do wait for few and then we can update it all together so not to paint again and again so overall performance high means update if again and again request and using this we can choose and set the priotrty to the reuest and all those things and reuse it later and then again play when needed all those things we can do.

Reconciliation is an algorithm that is used to differ between the browser dom and virtual dom so that they can update that particular root point.

So it is mainly used for the efficient transiting the state from one to another.In tree some component there are some thing that it is not necessary to compare those component it is good to directly change that component even without comparing so it is well managed by fibre.

Diffing of list is done using keys by stable,predictable and unique. to basically improve the performance using the keys.

>> props : props makes the component reusable.like we made a card then we make it again. so in a normal workflow if we want that we can pass multiple arugments or values and each values takes up differently so that is called the props

function Card(props){
  return (
    <>
    <h1>My name is : {props.username}</h1>
    </>
  )
}


<Card username = "Abhishek"/> // so here we are defining component card and making it reusable and we can pass any properties in a card.
<Card username = "AP"/>

// so here we passed an  props which is already defined by the react means if we want that component reusable then we can do that and now for example 

In a component passing we can pass the myobject = {//here object or array} and then we can access those object and array using the props.and then goes on.

function Card({username = "AA"}){ // here we are directly taking the username and then putting in it and removed the props again and again with curly braces. here we are predifing values also - aa if not any value passed then take AA
  return (
    <>
    <h1>My name is : {username || "AA"}</h1>//here also we are doing the same thin`g either take user value or take "AA"
    </>
  )
} 

// onclick is a method that expect a function not a reference hence setColor // we can pass but we cannot pass ()=>setColor() // this we have to use hence it is important.

>>Hooks : (useRef,useEffect,useCallBack)

useCallBack helps basically to memoise the function so that we can call it for the multiple request for example if we have multiple callback then we can call those function again and again then we get the result.In a usecallback we have two things measns two things one is function that we need to implement and second one is the dependencies means that we need to rerender all those dependies all those changes.





import { useState, useCallback } from "react";

// here we are calling passwordgenerator() but we cannot call that since we cannot do it manually react will automatically does so that effective management we can do
function App() {

const [length,setLength] = useState(8);
const [numberAllowed,setNumberAllowed] = useState(false);
const [charAllowed,setCharAllowed] = useState(false);
const  [password,setPassword] = useState("");


const passwordGenerator = useCallback(()=>{
  let pass = ""
  let str = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz";
  if(numberAllowed) str+="0123456789"
  if(charAllowed) str+="!@#$%^&*()_+?><:"

  for(let i =1;i<=length;i++){
  const index = Math.floor(Math.random() * str.length +1);
  pass+=str.charAt(index);
  }

  setPassword(pass)
},[length,numberAllowed,charAllowed,setPassword])

// passwordGenerator()

  return (
    <div className="App">
      <h1>Password Generator</h1>
      <hr></hr>
      <input type = "text" value={password} placeholder="Enter the passwod" readOnly/>
      <hr></hr>
      <button>Copy</button>
      <hr></hr>
      <div>
        <input type ="range" value={length} min={6} max={100} onChange={(e)=>setLength(e.target.value)}/>
        <label>Length : {length}</label>
        </div>

        <hr></hr>
        <div>
      <input type ="checkbox" defaultChecked={numberAllowed} onChange={()=>setNumberAllowed((prev) => !prev)}/>
      <label>Number</label>
      </div>
      <div>
      <input type ="checkbox" defaultChecked={charAllowed} onChange={()=>setCharAllowed((prev) => !prev)}/>
      <label>Character</label>
      </div>
    </div>
  );
}

export default App;


>> since in react we cannot call whenever we need to tell react that this function needs to be called and then depending upon best requirement it can be called .
// usecallback me aisa karna hai jo jo dependencies hai us method ko optimise karke kahi cache karlo jisse easy hoga aur useEffect matlab dubra se run kardo agar inme kisi me change ho raha hai to (memorise the function jitna ho sake)


>> useRef : when we need to take the reference of anything then useRef is used means if we want reference then we should use that.

const passRef = useRef(null) // this is the reference we are creating but here in reference we are passing null value we can pass any it is possible like useState


>> Custom hooks : So basically hooks are nothing they are just function who are returning array of constant value and the callback function using that we can effectively manipulate the UI. so it means we can also create some custom hooks using function.

>> Component : So basically component are part of the entire user interfaces and then that can be reusable also. 
Two types of Component :-
1. Functional Component : It is basically a function that is returning some html content and that can be render.(Stateless)
2. Class Component : It is another component that is a class component which extends React.Component and this class must render() html code like (Stateful)

class Component extends React.Component{
render(){
return (
<h1>Hello</h1>); // So here we are extending and rendering the component.
}}

>> Class Component : They are basically a es6 classes they can receive input(props optionally) and then can give output html. It can maintain a private internal state and use that private internal state information there only to do something.They are called as stateful because they can maintain their own data or we can say state hence they are called as functional component but now hooks can be used so the functional component are now also stateful.

JSX Stands for the javascript xml it is used to write xml inside the js code it is very much useful to write a good code means without it we can do.
props.children are used to have the children component. mean if we write the inbetween the component and then we can use props.children then we can do that.


>> Props in class component : Here props are used to have and it is same as the jsx code but instead of passing props to function we can access the properties using the this.props.and then whatever name we are specifying or we can say that this.props // which is reserved and then we can this.props.children whatever we want.

we cannot assign props properties explicity means like props.name = "something" // we cannot do that it is not possible

>> State : It is possible to control what is render on the screen either through props or state. One more thing props are passed within a function parameter and hence they have not control outside but state component entirely belong to the class and hence have the full control everywhere hence it is easy means props are not reassigned but props can be reassigned as they entirely belong to props . We can manage the state in class component by this.state and in functional component using useState.

Inside class component : We need to create a state object and then intialise that state object inside constructor. Whenever we have something that needs to executed after the state has been change then do not write it outside instead of that write inside a callback function which is the next parameter for a setState when we are setting a value. when we have somethin like calling again and again same thing so react gives all the thing together means it does not give all the 5 times for example incrementing 5 times a same counter so react send in a batches . In those condition always utilise a pevious state means never directly update the function pass the function in a setState or useState and then using prev value does the thing 

import { Component } from "react";

class Counter extends Component{
    constructor(){
        super()
        this.state ={
            count : 0
        }
    }

    incrementCounter(){
        // here another paramter is callback value that anything after function call then we do all these
        this.setState(
            {
            count : this.state.count+1
        },
        ()=>{
            console.log(this.state.count, " This is after the callback")
        })

        // here what happens function get called and it happens in a synchrounous way hence gettting value =0 because setState is a asyn operation
        console.log(this.state.count)
    }
    // second object is props object here if props object dependency
    incrementFiveTimesCounter(){
         // here another paramter is callback value that anything after function call then we do all these
        this.setState((prevState,props)=> ({
            count : prevState.count +1
        }))

        // here what happens function get called and it happens in a synchrounous way hence gettting value =0 because setState is a asyn operation
        console.log(this.state.count)
    }

    incrementFive(){
        this.incrementFiveTimesCounter()
        this.incrementFiveTimesCounter()
        this.incrementFiveTimesCounter()
        this.incrementFiveTimesCounter()
        this.incrementFiveTimesCounter()
    }

    render(){
        return (
            <>
            <h1>Counter - {this.state.count}</h1>
            <hr></hr>
            <button onClick={()=>this.incrementFive()}>Update the counter</button>
            </>
        );
    }
}

export default Counter


>> Event Handling : (Binding event handler in this function) so it can understand and does the work...

  <button onClick={()=>this.changeState()}>Click here State Class Component</button> // it has some performance implicatipn
  <button onClick={this.changeState.bind(this)}>Click here State Class Component</button> // it has some issue as if large event mean big application then issue
 another way inside 

constructor(){
this.changeState = this.changeState.bind(this)
}

// now normal call   <button onClick={()=>this.changeState}>Click here State Class Component</button> // this gonna work perfectly
... Now if we do not write this or do not bind the object then that this would be undefined when we update the setstATE hence we need to bind it 
we can pass method in props means anything inside the props means props or we can say function we can pass if any using props 


>> Conditional Rendering : When you want to render HTML on the basis of certain condition then we use this it is same as the condition works in js.
1. if/else
2. element variables
3. ternary condition operator
4. short circuit operator

.. using if/else we have to write inside the render() method it is not allowed to write anything inside jsx

>> List Rendering: Common thing to list the list of items means array of items

const ls = [1,2,3]
const newls = ls.map((x) => <h1>x<h1/>)// this is the way to iterate and then does the rendering means it is normal we are returning normal jsx and then placing inside jsx

>> List and Keys :
so what happens to match the keys since think if we have list of children and if order same then react will add only but if not order same then react will not render whole hence key attribute passed as props but we cannot access the key as properties as this is fixed and we cannot change that hence to avoid rerendering react matches the keys if it matches then not rerender again and again only necessary item delete add or update like this

Using the index as a key what happens when we create a list and have a index as a key then in normal it works it is good but when we want to change the order or sort it then it maintains an index so it fills out those points so what happens values stayed there yes index everything work fine but not values since it stores the value at first index and then at second and then goes on.

So we should not use index as a key until we have :
1. unique id 2.static list and will not change 3. list will not be reorder or filtered

>> Styling of css can be done either by styling inline by styling = whtever classname or we can do using className attribute

>> Lifecycle Method : When we create a component then it goes to several stages means react provide a method at different stages and using that we can edit at different stages.These lifecycle methods are available for the class component not for the functional component.

We can classify the methods into 4 phases :
1.Mounting : when instance of an component has been created and inserted into the DOM.
2. Updating : when component being re rendered either to the change of the state or props.
3. Unmounting : when component is being removed from the dom.
4. Error Handling : when there is an error in rendering or in any constructor in a children component.


>> Mounting Lifecycle Method :
1. Constructor(props) // so basically it is a function that will get called whenever new component is being created. It is used for intialising the state or binding the eventHandler(). if we are not calling super() inside this then we have to use this. otherwise we cannot do that and constructor is only place where we have access to the this.state otherwise apart from this we always have to use this.setState

2. static getDerivedStateFromProps(props,state) : This method is rarely used lifecycle method as said by react documentation.This method called when the state of the component depend on changes in props. set the state here do not have access to this keyword we need to return object here directly 

3. render() : only required method . here we should not not change state inside render also interact with dom.

4.componentDidMount() : this method called only once in a  whole lifecycle method and invoke immediately after the component and have children component has been rendered to the DOM. This method is perfect to load data and does all the things when interacting with the nodes. when component did mount called so it happens in a reverse order means internal that is child execution first and then parent and then so on goes

>> Updating LifeCycle Method : It appears in a order in which they appear. It gets called when the component is being re rendered on the basis of the properties or state change. It has 5 methods in which 3 are rarely used.

1. static getDerivedStateFromProps(props,state) // this method gets called whenever the component being updated or rerendered it has to return the null object or return the new object of component representing the current state.

2.shouldComponentUpdate(nextProps,nextState) // this method called to decide whenever there is update or should i change the component or not by default if any of the state or props changes then this method gets called. it return false or return true should upate or not by comparing prev props and state with cu

3. render() : same as change to update

4. getSnapshotBeforeUpdate(prevprops,prevstate) // this method called  right before the changes have been reflected in a dom before the virtual dom. it will ereturn the null or the value

5. componentDidUpdate(prevprops,prevstate,(value from the above getsnapshtbeforeuodate)) // this method called when the render happens in a rerender cycle properly .. here this is 100 percent sure that this method called only once



>> Unmounting phase method : componentwillunmount() this method invoke immediately before the component is bein destroyed and removed from the dom.Here we can do cleanup work like closing network and all those.

>> errorHandlingPhase : these method called when there is any error in component and or in an constructor of parent.
1. static getDerivedStateFromError(error) 
2. componentDidCatch(error,info)

>> Fragments : fragment basically helps let you group children element with being created extra node in a dom. 
 // so basically here fragment means without creating entry to the dom and grouping children . Here one thing is sure that <React.fragment> or <> normal this we have to use in order to do fragmentation and only paramtere that is allowed inside React.fragment is a key = {} // any key that can be used for grouping the table content.

import React, { Component} from "react";
import { Fragment } from "react";
class Fragments extends Component{
    render(){
        return(
            // so basically here fragment means without creating entry to the dom and grouping children
            <React.Fragment key = id> // here id is allowed not anything more than that then we can do whateve we wamt//
            <h1> What is your name ? </h1>
            </React.Fragment>
        )
    }
}

export default Fragments

>> Pure Component in react :

One way is a class based component is to extend from the component another way is to extend from the pure component class. Regular Component does not implement shouldComponentUpdate() method by default it always return true where as in case of Pure Component does the immediate shallow props and state comparison. 

In regular always calls as it always return true while in case of purecomponent comparison if the state and props have sma value then does not return true hance called only once. shallow comparison means for the primitives a and b has the same data type and the value and same type like string while in complex data type it has the same reference.

>> Memo : PureComponent only re renders something when there is a difference in a shallow comparison between the props and state for the performance optimisation and here it only works for the class based component . So to achieve this functionality in a functional based component we have something called as memo

import React from "react"
function MemoComponent({name}){
    console.log("Memo Component Render")
    return(
        <>
        <h1>My name is : {name}</h1>
        </>
    )
}

export default React.memo(MemoComponent)

.// so here we are using React.memo(inside component name) so this will basically give me a higher order component that will only re render only if there is change in a props and state. so basically it is introduced and worked after react 16 so use that so basically for the performance improvement we do this.

>> refs in a react : refs make it possible to access dom node directly within react.(like focusing a text input) when page loads then input name should be focused. So it is basically used to access a dom node . it is basically an object in which it holds the current dom node object.

So we have three steps to do :
1. create ref in constructor so that it is accessible to entire component using React.createRef() method 
2. assign ref to whatever html tag or node we want reference by passing attribute ref = {this.ref_nmae}
3. call the method using current this.inputref.current.method_name // whatever we want to do

Right now we are adding reference to the html element means a normal html element or tag but is possible to add reference to the class element. so basically we are passing input to the parent component to child means from one by one that is attaching references to the ref


>> Forward Ref : Ref forwarding is a technique of automatically forwarding the ref from a component to a one of its children. so basically we can achieve this using React.forwardRef() method that takes paramters as a component that is going to receive a refernce from the component.


>> React Portal : React Portal are used to render the children into a dom nodes that exists outside the component hierarchy of the parent component. So basically in a react everything is inside the root component and app is inside that and everything inside that so basically means if we want to render some nodes without in the dom hierarchy then we can use the portal. So basically we can do this using ReactDom.createPortal() // it takes two parameters first one can be anything number,string ,jsx ,component that you want to render where as second parameter is the target means document.getElementbyId("portal-root") where you want to render.

So basically in a react portal also event bubbling happens when an node is fired from the dom tree then event bubbling happens and pass it to the parent like these


>> Error Boundary : So basically what happens when we do a normal and if any error then it can unmount the entire dom tree and breaks the code entirely so to handle those thing we do a error catch and then show afallback UI.
a class component that either implements one or both of the error lifecycle method that is called the error boundary. getDerivedstatefromerror method is used to render a fallback ui after an error is thrown and componentdicatch is basically used to log the information.


// this error boundary catches the error in rendering, lifecyle method , constructor of the whole tree but does not catch at a event handler for that you need to use normal if else statement.

>> Higher Order Component :
So basically what happens in when we are writing the same functionality for so many times then we are not reusing the same code we are simply duplicating the code what we want that we should reuse it again so what we can do if both are present in same parent state then it will work but if both are not in same parent then that becomes problem so to avoid this we have to use higher order component. So immediate thought is to lift this to parent component and then reuse by passing props but it is not always possible if they are so much far apart and higher in number hence we have to use something called as higher order component.

To share common functionality between component we use the higher order component.

So basically higher order component are the enhanced component or advance component that we receive from the passing original component in some function and getting a more advance component.

import { Component } from "react";

const UpdatedComponent = (OriginalComponent) =>{
    class NewComponent extends Component{
         constructor(props){
        super(props)
        this.state = {
            count : 0
        }

        this.incrementCounter = this.incrementCounter.bind(this)
    }


    incrementCounter() {
        this.setState((prevstate)=>{
            return {count : prevstate.count + 1}
        })
    }


        render(){
            return(
                <>
                <OriginalComponent incrementCounter = {this.incrementCounter} count = {this.state.count}/>
                </>
            )
        }
    }
     return NewComponent
}

export default UpdatedComponent


>> when we are passing props in a hoc function so it gets wrapped to the higher order component means if we do this.props some other component then that is not accessible so we have to use {... this.props} inside a higher order component at a bottom line so that it can be passed to a other original component . in a hoc we can pass multiple parameters also so that we can do whatever we want to do.


>> Render Props : This is the another pattern inorder to use the common functionality as we do in a higher order component. In react it is possible to use prop whose value is a function to control what is actually rendered by the component. Render props are technique to share the code between two component using a prop whose value is a function.


>> Context Api :
Consider the react application has so many component nested at several level if we want to provide a certain props then we need to pass at each level then it goes to that level so to avoid this we use the context api which is a way to pass the data through the component tree without having pass down props manually at each level.

so basically it has the three steps to do this :
1. create a context 2. provide a context value 3. consume a context value by the necessary component . we can set thedefault value to a context

import React from "react";

const UserContext = React.createContext("Abhishek") // now if we will not use userprovider so while consuming it will use the byfedaultt value = "Abhishek"


// so basically we are creating provider and consumer so that we can consume and provide value
// we need to do do userprovider to whome so that value available to all children otherwise not
const UserProvider = UserContext.Provider
const UserConsumer = UserContext.Consumer



export {UserProvider, UserConsumer}



import { Component } from "react"
import ComponentF from "./ComponentF"
import UserContext from "./UserContext"
class ComponentE extends Component{
    static contextType = UserContext
    render(){
        return(
            <>
            <h1>
            value using context-type : {this.context}
            <ComponentF/>
            </h1>
            </>
        )
    }
}
// we are using context class in order to access the value directly using contexttype and then access using this.context
// ComponentE.contextType = UserContext
export default ComponentE // ......>>>

>>>>.... this is the simplest way using context but we cannot do in real as we might have so many context in which consumercomponent is good

>> HTTP AND REACT :
So basically react is a library it does not need to worry anything about the http request .It even does not know about the sever it only knows about the state and props and depending upon state and props it renders the data it just need a data in props and state. Now react is not going to handle http request. So here you have to make use of http library which  will handle the request. (axios and fetch) are those with whome we are going to fetch a data.we will get and add the axios package to the application and then does the use of axios library in order to handle http request and fetch the data or send the data.

To install axios run the command npm install axios 


// Get Request using axios and render the fetch data :

   componentDidMount(){
        axios.get('https://jsonplaceholder.typicode.com/posts')
        .then((res)=>{
            console.log(res)
            this.setState({
                posts : res.data
            })
        }).catch((err)=>{
            console.log(err)
        })
    }


>> Posting a data from react to somewhere :
 axios.post('https://jsonplaceholder.typicode.com/posts',this.state) // here the second param is whatever the data we are going to send as a paylod then we are going to get response or catch the error means same as above


............................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................................
>> Hooks : Hooks are used to use advance features of react without the classes. hooks does not work inside the class they work outside the classes.
Use of this keyword, and also binding eventhandler is problem.Hooks are used to basically make our work easy as simple as that.

.. useState() hook : chai aur code is same as this
// rules of hooks is that : write hooks at very top level and never call hooks inside the loops,condition or nested function, only call hooks from react function


const[name,setName] = useState({firstname : "",lastname :""})
// so what happens when we implement this so by default usestate does not split the firstname and lastname we have to do it manually otherwise if i write one then it will take that only will not take other hence to avoid this we have to use spread operator that copy the entire object and change the particular value

setName(...name, firstname : Harsh) // so this is copying the entire name and then only changing the firstname so this will work otherwise objedct are themselves not distributable.


>> useEffect Hooks ::::
So basically what happens when we want to do like when button value gets updated then show that update thing in a document title if we want then we have to use componentdidupdate and does all the things and while wrting in a componentdidmount so basically we are writing the same code again and again to avoid these and put together all the necessary things together we use the useffect hook. it is close replacement of componentdidupdate,mount,unmount so for performing side effects in functional component we need the useEffect hook.

So what happens useeffect renders every time a component render. after every render useeffect runs . It is placed inside the function. After every render runs so it might give a problem of performance so we need to set a condition in which this useeffect is going to run.

so basically we can add dependencies [count] as a second parameter that when these values changes then run the useEffect.so basically for calling once the useffect initial only use an [] empty array as a second parameter so that will work passing it will only call once. we are mimickin the componentdidmount() here since it is calling only once.


// now doing componentwillunmount() idea means for the cleanup purpose :
when you want to do cleanup code in useffect then 

inside useEffect(()=>{
// write the code

return ()=>{
console.log("Unmounting code");
// write the removable code it will automatically removed after calling once and remove whatever thing necessary
}
},[])


>> useEffect With incorrect dependency : means always pass dependency on whome the useEffect is depending 

>> Fetching the data with useEffect :write inside the fetching logic and pass [] otherwise it will fetch for the infinite time or any dependency if dependent on dependency.

....>>> useContext Hook 

So basically in a context api when we have so many nesting like context then we have lots of value then that becomes a problem so to avoid that useContext is uses

<UserConsumer>
                {
                    user =>{
                        return(
                            <ChannelContext.Consumer>
                                {
                                    channel =>{
                                        return <div>Hello {user} from {channel}</div>
                                    }
                                }
                            </ChannelContext.Consumer>
                        )
                    }
                }
            </UserConsumer> 

// so here lots of nesting is there so basically avoid this we use the useContext hook.

so basically in normal context api we create,produce and then consume the context all the process of creation and production is same the only difference here in case of consumption we have to use useContext means here we need to do importusecontext and then import all the context we have created and then pass the context created to usecontext hook and use it simply

import { useContext } from "react";
import UserContext from "./UserContext";
import { ChannelContext } from "../App";

// this we are doing and using the useContext inorder to consume the values
function FunctionalComponentContext(){
    const user = useContext(UserContext)
    const channel = useContext(ChannelContext)


    return(
        <div>
            {user} by functional context :::: {channel}
        </div>
    )
}

export default FunctionalComponentContext


>> useReducer hook : it is a hook that is used for the state management. it is an alternative to usestate. usestate is build using the usereducer.
so basically this hook is somewhat related to the reduce method in a array.reduce(reducer,5) // so here reducer is the function wirhparamrer acc and current value similarly in case of useReducer(reducer,initialstate) in reducer we give (currentstate,action) and we get the new state and that state has been passed into useReducer that gives the newstate and dispatch method to specify the action.


Actions are basically the instruction to the reducer based on those action reducer function goes and does the transition from one state to another.
we can use multiple reducer to do as per needed and we can reuse the same piece of reducer and initial state both will be independent of each other means everything different state and reducer will be actually different.


>> Currently we have used useReducer with the local sate component means within the component now we want to use useReducer with the global state means outside the component with the help of useReducer and useContext.

We need to implement one counter and want that every component maintain the same counter value to do that we use the useredcuer + usecontext method
import logo from './logo.svg';
import './App.css';
import CounterUseReducer from './Components/CounterUseReducer';
import CounterObject from './Components/CounterObject';
import React, { useReducer } from 'react';
import ComponentD from './Components/ComponentD';
import ComponentE from './Components/ComponentE';
import ComponentF from './Components/ComponentF';


const initialState = 0;

const reducer = (state,action) =>{
    switch(action){
        case 'increment':
            return state+1
        case 'decrement':
            return state-1
        case 'reset':
            return 0
        default:
            return state
    }
}

const Appcontext = React.createContext()

function App() {
  const [count,dispatch] = useReducer(reducer,initialState);
  return (
    <div className="App">
      {/* <CounterUseReducer/> */}
      {/* <CounterObject/> */}

      Count - {count}
      <Appcontext.Provider value = {{countC :count, countDispatch : dispatch}}>
      <ComponentD/>
      <ComponentE/>
      <ComponentF/>
      </Appcontext.Provider>

    </div>
  );
}

export default App;
export {Appcontext}




>> Fetching the data with useReducer () :-

import { useEffect, useReducer } from "react"
import axios from 'axios'

const initialState = {
    loading : true,
    error : '',
    posts : {}
}

const reducer = (state,action) =>{
    switch(action.type){
        case 'FETCH-SUCCESS':
            return {
                loading : false,
                error : '',
                posts : action.payload
            }
        case 'FETCH-ERROR':
            return {
                loading : false,
                error : 'Something went wrong!',
                posts : {}
            }
        default:
            return state
    }
}
function FetchUseReducer(){
    // dispatch is used to call the function reducer or specify the action like here increment decrement or somehthing like this
    const [state,dispatch] = useReducer(reducer,initialState);

    useEffect(()=>{
    axios.get("https://jsonplaceholder.typicode.com/posts/1")
    .then((res)=>{
        dispatch({type: 'FETCH-SUCCESS', payload:res.data})
    })
    .catch((err)=>{
        dispatch({type: 'FETCH-ERROR'})
    })
    },[])
    return (
        <>
        {state.loading ? 'Loading' : state.posts.title}
        {state.error ? state.error : null}
        </>
    )
}

export default  FetchUseReducer

>> usereducer vs useEffect :::---- usestate - primitive data type, one or two transition, not related transition ,less complex logic and have no business logic ,local state vice versa for usereducer.

>>usecallback : callback hook is used for the performance optimisation. So basically what happen when we create a normal code and its state and props changes then rendering happen so everytime even if state or props changes always rerendering happens so to avoid this we use something called as React.memo(component) // but here everything fine but if function are passeds as props then everytime something changes then it will  create a new render funtion even if function not changing still it will create a new function hence always re rendering happens here in case of function props to avoid this we have to use usecallback hook.

usecallback is a memoisation function that will store the callback funtion and will cache it and it will return the same function until the dependencies have not been changed/


import React from "react"

function Count({text,count}) {
    console.log(`Rendering ${text}`)
    return (
       <div>{text} - {count}</div>
    )
}

// this will pretent that only if the props and state changes then the method should call but here in case of
// the function it would not be able to do as always function gets newer hence callback hook is used if any of the dependencies changes then it will create a new function otherwise will memoise the same function
export default React.memo(Count)

   // here we are using usecallback so that if dependencies changes then only do the function change otherwise give same
    const incrementAge  = useCallback(()=>{
        setAge(age+1);
    },[age])

    const incrementSalary = useCallback(()=>{
        setSalary(age+1);
    },[salary])

>> useMemo hook : it is also concerned with performance optmisation. It is a hook that get recalculated when one of the dependencies get changed to avoid expensive calculation to every render. When we want to cache a function itself then we can use usecallback and when we want to cache the function value then we have to use useMemo hook. 
cont val = useMemo(()=>{},[age]) // dependency of age so it will cache the value of function val and then recalculate if dependency changes


>> useRef : it is simply same as above inorder to have the same reference everywhere and then inputref = useRef(value) >>>. inputref.current // to get current and does whatever you want to do

>>  Custom Hooks : Custom hooks are basically used to share the functionality in order that they can be reusable and become the as non repeatable as possible.They are generally component where we write our logic and then get the required function by calling it and then use that.


............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................

>> React Redux :- Redux is a predictable state container so basically it manages the state in a very effective. It is for the entire js application not only for the react. So basically when one want to share something then it has to lift it up and then pass it as a props to children. So basically if lot of distribution then state management. So basically to avoid this we use a react container that will hold the state and then does the necessary operation and then atlast return awhatever necessary things. 

Redux is a state management library that we have to use that maintains and manages the state.To directly use redux it is tough to implement.So to avoid this we use React-redux ui binding library for react. For sharing of a state we the redux. (npm install redux is a code to install redux )


>> Three Core Concepts :
1. Store : that holds the state of your application just like a shop holds a cake.
2. Action : describes what needs to be performed like reduce cake by 1	.
3. Reducer : that actually carries out the state transition depending on action and current state of application like shopkeeper who does the all the things

>> Three Principal :
1. the state of your application is stored in an object in a single tree .Maintain our application state in a single object would be managed by redux store.

2. The only way to tell redux what to do is with the help of actions.

3. we need to write reducer inorder to do the state transition and pass a current state and action together inorder to get a new state.

You can directly update the state via js applications or there you need to dispatch actions and then reducer update and then does the state and then goes to apps.


>> Actions : they are basically js object and must have type properties and also they are only way to connect to js store.so we need to create a Actioncreator they are basically a function that will create an action means function that will return an action  object

>> Reducer : are basically a function that will take actions and state as an argument and return a new state 
 

>> Redux Store  : it is something that brings the action and reducer together. for an entire application we have only redux store.
..1. holds the application state
..2. allow access to state via getState()
..3. allow state to be update via dispatch(action)
..4. register listener via subscribe(listener)
..5. unregistering of listener via the function return by the subscribe.

// so here we are doing redux thing in order to mange the application
/// HERE WE ARE DOING WITH THE ONE REDUCER
console.log("Redux demo")

// so basically to combine the reducer we have something called as combine reducer provided by redux
const redux = require('redux')
const createStore = redux.createStore
// here we are combining the reducer and then passing the both the reducer as key value 
const combineReducer = redux.combineReducers

const BUY_CAKE ="BUY_CAKE"
function buyCake(){
    return{
        type : BUY_CAKE,
        infor : "First redux function"
    }
}

const BUY_ICECREAM ="BUY_ICECREAM"
function buyIcecream(){
    return{
        type : BUY_ICECREAM,
    }
}

const initialstatecake = {
    cakes : 10,
}


const initialstateicecream = {
    icecream : 20
}

const reducercake = (state = initialstatecake,action)=>{

    switch(action.type){
        case BUY_CAKE:
            return{
                ...state,
                cakes : state.cakes-1
            }
        default: return state
    }
}


const reducericecream = (state = initialstateicecream,action)=>{

    switch(action.type){
        case BUY_ICECREAM:
            return{
                ...state,
                icecream : state.icecream-1
            }
        default: return state
    }
}

// passing both the reducer as a key value pair in a combine reducer so that both works fine
// here both the reducer recieves tha action where as one acts on action whereas other one ignores that action
const reducer = combineReducer({
    cake : reducercake,
    icecream : reducericecream
})

 // holding the application state  
const store = createStore(reducer)

// getState method
console.log('Intial state',store.getState())

// we are getting the value unsubsribe and then doing all the function
const unsubscribe = store.subscribe(()=>{console.log('Updated state', store.getState())})
store.dispatch(buyCake())
store.dispatch(buyCake())
store.dispatch(buyIcecream())
store.dispatch(buyIcecream())
store.dispatch(buyIcecream())
store.dispatch(buyCake())

unsubscribe()


>> Middleware : it is a way to extend redux with custom functionality. it provides third party action between dispatching an action and reaching it to reducer. use middleware for logging,crash report, perform asynchronous task .. so right now we are using and incorporating something called as redux-logger 

// logger for an application
const reduxlogger = require('redux-logger')
const logger = reduxlogger.createLogger()

// to apply middle ware redux library have an method called as applymiddleware
// so here we are doing redux thing in order to mange the application
/// HERE WE ARE DOING WITH THE ONE REDUCER
console.log("Redux demo")

// so basically to combine the reducer we have something called as combine reducer provided by redux
const redux = require('redux')
const createStore = redux.createStore
// here we are combining the reducer and then passing the both the reducer as key value 
const combineReducer = redux.combineReducers


// logger for an application
const reduxlogger = require('redux-logger')
const logger = reduxlogger.createLogger()


const applymiddleware = redux.applyMiddleware

const BUY_CAKE ="BUY_CAKE"
function buyCake(){
    return{
        type : BUY_CAKE,
        infor : "First redux function"
    }
}

const BUY_ICECREAM ="BUY_ICECREAM"
function buyIcecream(){
    return{
        type : BUY_ICECREAM,
    }
}

const initialstatecake = {
    cakes : 10,
}


const initialstateicecream = {
    icecream : 20
}

const reducercake = (state = initialstatecake,action)=>{

    switch(action.type){
        case BUY_CAKE:
            return{
                ...state,
                cakes : state.cakes-1
            }
        default: return state
    }
}


const reducericecream = (state = initialstateicecream,action)=>{

    switch(action.type){
        case BUY_ICECREAM:
            return{
                ...state,
                icecream : state.icecream-1
            }
        default: return state
    }
}

// passing both the reducer as a key value pair in a combine reducer so that both works fine
// here both the reducer recieves tha action where as one acts on action whereas other one ignores that action
const reducer = combineReducer({
    cake : reducercake,
    icecream : reducericecream
})

 // holding the application state  and we are here passing the logger in a applymiddleware
const store = createStore(reducer,applymiddleware(logger))

// getState method
console.log('Intial state',store.getState())

// we are getting the value unsubsribe and then doing all the function
const unsubscribe = store.subscribe(()=>{})
store.dispatch(buyCake())
store.dispatch(buyCake())
store.dispatch(buyIcecream())
store.dispatch(buyIcecream())
store.dispatch(buyIcecream())
store.dispatch(buyCake())

unsubscribe()


>> Async Actions : Synchronous actions are one with that are as soon as dispatched then it will update the state.so basically we will use the redux thunk and also the axios inorder to correctly fetch the data.

thunk middle allows you to create a action as a function instead of an action object.it does not need to be pure it means it can do async operation and all other things this can do.
const redux = require('redux')
const { createStore, applyMiddleware } = redux
const thunkMiddleware = require('redux-thunk').thunk
const axios = require('axios')


const initialState = {
    loading : false,
    users : [],
    error : ''
}

const FETCH_USERS_REQUEST = "FETCH_USERS_REQUEST"
const FETCH_USERS_SUCCESS = "FETCH_USERS_SUCCESS"
const FETCH_USERS_FAILURE = "FETCH_USERS_FAILURE"

function fetchUsersRequest(){
    return{
        type : FETCH_USERS_REQUEST
    }
}

function fetchUsersSuccess(users){
    return{
        type : FETCH_USERS_SUCCESS,
        payload : users
    }
}

function fetchUsersFailure(error){
    return{
        type : FETCH_USERS_FAILURE,
        payload : error
    }
}


const reducer = (state = initialState,action) =>{
    switch(action.type){
        case FETCH_USERS_REQUEST: return{
            ...state,
            loading :true
        }

        case FETCH_USERS_SUCCESS: return{
            ...state,
            loading :false,
            error : '',
            users : action.payload
        }

        case FETCH_USERS_FAILURE: return{
            ...state,
            loading :false,
            error : action.payload,
            users : []
        }
        default: return state
    }
}



const fetchUsers = ()=>{
    return function(dispatch){
        dispatch(fetchUsersRequest())
        axios.get('https://jsonplaceholder.typicode.com/users')
        .then((response)=>{
            const users = response.data.map(user =>user.id)
            dispatch(fetchUsersSuccess(users))
        })
        .catch((error)=>{
            dispatch(fetchUsersFailure(error.message))
        })
    }
}
const store = createStore(reducer, applyMiddleware(thunkMiddleware))

const unsubscribe = store.subscribe(()=>{console.log(store.getState())})
store.dispatch(fetchUsers())

>> React Redux :
now till the same as we have created a redux store now we have to provide this store to a react application via provider (react-redux) . Provider is a component that has been provided by react-redux in order to get redux store to react application. typically wherever we provide the store it wpuld be available tp avery component. Now we have provided a store to the react application now we want to connect and then does the work.


.... mapStateToProps(state) as an parameter and return an object and selector are used to so that we can have something to select and we can use selector file if we have large number of data and we want selective things make selectior.js and then does the calculation atlast take whatever necessary

... mapDispatchToProps(dispatch) and returnd an object 

so basically it will get more props from the component apart from it is getting and dispatch method also it is getting the another props that we have declared actions and state and reducer also using 
 
import BuyCake from "../redux/cakes/CakeActions";
import {connect} from  'react-redux'

function CakeContainer(props){
    return(
        <>
        <h1> Number of Cakes - {props.numOfCakes}</h1>
        <button onClick={props.buycake}>Button</button>
        </>
    )
}

const mapStateToProps = state =>({
    numOfCakes : state.numOfCakes
})

const mapDispatchToProps = dispatch =>({
    buycake : () => dispatch(BuyCake())
})

export default connect(mapStateToProps,mapDispatchToProps)(CakeContainer)
Using connect we are connecting the react-redux and everything

>> Redux with hooks : So basically redux with hooks allows you to do send get and dispatch without using the connect and subscribing to store and dispatching could easily bed done by redux with hooks. How to use hooks instead of connect

.. useSelector : it is a hook that is close equivalent to the matchstatetoprops method that maintains the state that is present in a store return value
.. useDispatch : it is a hook that is close equivalent to the matchDispatchtoprops method that maintains the state that is present in a store return func

import {useSelector, useDispatch} from  'react-redux'
import BuyCake from '../redux/cakes/CakeActions'
function HooksCakeContainer(){
    // this is a way we are connecting to a store of each one means instead matchStatetoprops 
    const numOfCakes = useSelector(state => state.numOfCakes)
    const dispatch = useDispatch()
    return(
        <>
        <h1> Number of Cakes - {numOfCakes}</h1>
        <button onClick={()=>dispatch(BuyCake())}>Button</button>
        </>
    )
}
export default HooksCakeContainer


>> Logger Middleware in react : npm install redux-logger
>> Redux devtool extension : it is an helpful tool for an react application redux.
1. add extension to your browser
2. redux-dev-tool to a react application using  npm install @redux-devtools/extension

import { composeWithDevTools } from '@redux-devtools/extension'; and then do that const Store = createStore(reducer, composeWithDevTools(
    applyMiddleware(logger)
  ))


>>Action Payload : it is basically used to send a data means from one to another and then required action... using function value 

............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................

>> mapStateToProps: it basically accepts two parameter one is its state and second one is ownprops that we pass and depending on our own props we can decide what to send what to do import {connect} from  'react-redux'

function ItemContainer(props){
    return(
        <>
        <h1> Items - {props.item}</h1>
        </>
    )
}

const mapStateToProps = (state,ownProps) =>{ 
    const itemstate = ownProps.cake ? state.cake.numOfCakes : state.icecream.numOfIceCream
    return {
        item : itemstate
    }
}

>> mapDispatchToProps : here also we can pass our own props and we can dispatch and do the same thing as needed by the function means smae thing



............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................
............................................................................................................................................................


>> Advance Topics :: Whenver rerender happens then new virtual dom created so basically react detect the changes from a previos virtual dom to current virtual dom and then that changes if there will be propogated to ReactDom and then Reactdom will propagate the changes to original dom and communication.
If react directly changes real dom then upon every rerendering it will create a new dom element and changes and same and again it is same as normal js. 
Only the function call rerender ionly component whose value changes or you can say that component and only ther child created not parent if not required.

While comparing the primitive has been compared on the basis of values where as the non primitive like object,array has been compared on the basis of references. so whenever we like writing so while setting setState([...arr,5]) // so here it is creating new array and then pushing so different reference and we see in the re rendering..


>> Diffing Algorithm : Comparison between the earlier dom tree and new dom tree and detecting changes is called diffing algorithm. In diffing algorithm there has been comparison on the basis of position. So basically what happens when there are two data like static and dynamic in any app so like
<h1> Counter - {count}</h1> // so basically it create a test node one is static data like counter and 0ne is dynamic data like count so it will change only the dynamic data this is how precise the react works.


Key : children(dyamic children we need to provide key that is unique and please do not give index number as key)
comparison always happen on the basis of position if not key but if key there then it compare on the bais of key if key present
::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::

>> React Testing Library ::
.. it is mainly used for checking weather everything working fine or not in a UI as this is a testing library works very well for the dom testing or you can testing the UI . it does not check for the internal working like this component has 5 state so check all the state it does not have anything to do with this. It only focuses on outside this means it has highly focus towards the UI (user interfaces).

so basically react have so many functions like test,it => these are used to describe the function means test and it are used to test inside this whatever we write our library checks for that it to test.

Now we have describe function that is used to basically bind so many test and it function that are actually testing in order to have a common name like we describe login test means login testing it is doing and inside that so many things are there.

>> So there are three specification on the basis of that we need to test like :
1. .test.js : like we have home.js then its testfile home.test.js and inside this write everything
2. .spec.js : it is same home.spec.js
3. __tests_ : here we have to make a folder __test__ and then inside this we have to do make a same name folder like we can have home.js inside this and then write inside the same code as above


>> Query Methods : query,find and get and query returns a  null if not present where as get and find by returns an error if not present. FindBy method is async operations hence we need to use await when using findby{whatever required) and then usng async outside.

queryByText(), queryByLabelText and so many things then we have to pass parameter and if we want that matching name present then we have to use second parameter {exact:false} and then if we want to test by the element tag then we have to use queryByRole("button",{name : "add"}) // like this we have to test.

expect(linkelement).tobeInTheDocument or expect(linkelement).toBeNull // means document me hai ki nahi or null hai ki nahi or we ca use tobe(//means inside this what is there)	


>>FireEvent : fireevent.click(btn) or it can be fireevent.change(inputvalue) // so basically we use the firevent to check weather this things happen or not so basically there are certain steps first get the element by screen.getelement and then fireevent and then get the message and then and then expect the content  // means there are steps you have to do and perform the steps . we need to include fireevent. 


>> waitFor : we need to include waitFor and then do and inside async await and then pass the parameter that it is checking wait for that much time if we will not include waitFor it will give you a error means we are checking right now that element is there if it is taking time then it should wait for some time then check if timelimit so much then wait for specific timeout and then execute whatever needed.

const btn = screen.getByRole('button')
fireevent.click(btn)

await waitfor(()=>screen.getTestById('li-1'),{timeout:100000}); // this is basically  we are doing that wait for this much time and it is using waitfor 
expect(screen.getTestById('li-1')).toHaveTextContent('Test List')

... jest.setTimeOut(timeoutvalue) // here means it should wait for this many seconds for the resonse of any 


>>> if we will use findBy then we do not need to use waitFor and then code here if we will use findby then it gonna work for the fine means it will work without using waitfor this gonna work fine 


>> Mock Service Worker : using the mock service worker it is basically since when we hit a api then it can have host also so basically to avoid this we can test the api as per need means in handlers we need to define post data we are expecting and then return ctx res req and then using that we can do and then we have to mocks inside that we have to use server handlers and then add in a setuptest and then does the thing you want to do by await method means it is same just check the content and mock service worker means we have to do as per our api then we have to use rest and rest.get rest.post 


rest.get('api',(req,res,ctx)=>{
return res(
ctx.json({
data : data we are giving 
name : "Code demo" // means it can be anything inside this means we can specify anything any data kuch bhi
}))


>>beforeAll(),afterAll(),beforeEach(),afterEach() ::

beforeAll(()=>{
//write here whatver needed // here we can write any function 
})

// so beforeAll,afterAll,beforeEach,afterEach they are function that are used basically inorder to have all means eitherbefore or after all the function and then each means before and after the each function































